Design a Notification System — Frontend System Design:

Assume the interviewer asks:
    Design a notification system for a large web application like LinkedIn, Facebook, or an EMR.

->For a frontend interview, focus on:
     how notifications reach the browser, 
     how they're stored/cached on the client, 
     unread counts, real-time updates, pagination, optimistic updates, and performance.

1. Clarify requirements:
------------------------
    I'd first establish that users should be able:
                     to receive notifications in real time, see an unread badge, open a notification panel, 
                     mark individual notifications as read, mark all as read, navigate to the relevant page, 
                     and load older notifications.

    Example:
                        🔔 3

              ┌────────────────────────┐
              │ Notifications          │
              │                        │
              │ ● John mentioned you   │
              │   2 min ago            │
              │                        │
              │ ● New appointment      │
              │   10 min ago           │
              │                        │
              │   Report completed     │
              │   Yesterday            │
              │                        │
              │      Load more         │
              └────────────────────────┘

    The important frontend requirements are real-time delivery, unread/read state, pagination, caching, deduplication, reconnection, and good performance

2. The system-design diagram:
-----------------------------
    This is the kind of diagram I'd draw in an interview:
                        USER
                          │
                          ▼
               ┌─────────────────────┐
               │    React App        │
               │                     │
               │ 🔔 NotificationBell │
               │ NotificationPanel   │
               │ NotificationItem    │
               └──────────┬──────────┘
                          │
                ┌─────────┴─────────┐
                │                   │
                ▼                   ▼
       ┌────────────────┐    ┌────────────────┐
       │ TanStack Query │    │   WebSocket    │
       │                │    │                │
       │ Cache          │    │ Real-time      │
       │ Pagination     │    │ notifications  │
       └───────┬────────┘    └───────┬────────┘
               │                     │
               │ REST API            │ WS
               │                     │
               └──────────┬──────────┘
                          ▼
                 ┌─────────────────┐
                 │ Notification    │
                 │ Backend         │
                 └────────┬────────┘
                          │
                          ▼
                     ┌─────────┐
                     │   DB    │
                     └─────────┘

    That diagram is enough to drive most of the interview discussion.
    The key idea is:
        REST API      → Existing/older notifications
        WebSocket     → New real-time notifications
        TanStack Query → Frontend server-state/cache

3. Component structure:
-----------------------
    I'd structure the frontend approximately like this:
        App
        │
        ├── Header
        │    └── NotificationBell
        │          └── UnreadBadge
        │
        └── NotificationPanel
            │
            ├── NotificationHeader
            │     └── MarkAllAsRead
            │
            └── NotificationList
                    │
                    ├── NotificationItem
                    ├── NotificationItem
                    └── LoadMore

    ->NotificationItem should remain reusable.
            <NotificationItem
                notification={notification}
                onRead={handleRead}
            />

4. Notification data model:
---------------------------
    